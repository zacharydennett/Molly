import { AdsDashboard } from "@/components/competitor-ads/AdsDashboard";

export const metadata = { title: "Competitor Ads — Molly The Monday Reporter" };

export default function CompetitorAdsPage() {
  return <AdsDashboard />;
}
