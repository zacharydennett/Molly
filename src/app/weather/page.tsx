import { WeatherDashboard } from "@/components/weather/WeatherDashboard";

export const metadata = { title: "Weather — Molly The Monday Reporter" };

export default function WeatherPage() {
  return <WeatherDashboard />;
}
